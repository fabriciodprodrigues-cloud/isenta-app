{
  "name": "isenta",
  "version": "0.1.0",
  "private": true,
  "description": "Plataforma para gerenciar isenções de pedágio de frotas públicas",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check",
    "db:migrate": "cd apps/web && pnpm exec prisma migrate dev",
    "db:seed": "cd apps/web && pnpm exec prisma db seed",
    "db:push": "cd apps/web && pnpm exec prisma db push",
    "db:studio": "cd apps/web && pnpm exec prisma studio"
  },
  "devDependencies": {
    "@types/papaparse": "^5.5.2",
    "papaparse": "^5.5.4",
    "prisma": "^5.22.0",
    "tsx": "^4.7.0",
    "turbo": "^2.0.0"
  },
  "dependencies": {
    "@next-auth/prisma-adapter": "^1.0.7",
    "@prisma/client": "^5.7.1"
  }
}

// Este arquivo é para lembrar de atualizar o Sidebar com o novo link
// Adicionar em apps/web/components/Sidebar.tsx dentro da seção de alertas:

/*
{session?.user?.role === 'admin' && (
  <Link href="/dashboard/alerts-config">
    <div
      className={`rounded-lg px-4 py-2 transition-colors ${
        isActive('/dashboard/alerts-config')
          ? 'bg-ink-700 text-green'
          : 'text-slate hover:text-paper hover:bg-ink-700/50'
      }`}
    >
      Configuração
    </div>
  </Link>
)}
*/
